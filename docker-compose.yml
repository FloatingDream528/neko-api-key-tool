services:
  neko-api-key-tool:
    image: ghcr.io/floatingdream528/neko-api-key-tool:latest

    # 从 .env 文件加载环境变量，容器启动时由入口脚本注入到前端
    env_file:
      - .env

    container_name: neko-api-key-tool
    restart: always
    ports:
      - "3456:80"
